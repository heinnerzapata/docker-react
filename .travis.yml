language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t heinnerzapata/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true heinnerzapata/docker-react npm run test
